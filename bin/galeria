#!/bin/bash
set -e
DIR="$(cd "$(dirname "$0")/.." && pwd)"
python3 "$DIR/bin/_galeria.py"
echo ""
echo "Para ver localmente (o app usa fetch, precisa de servidor):"
echo "  cd \"$DIR\" && python3 -m http.server 8000"
echo "  depois abra http://localhost:8000"
